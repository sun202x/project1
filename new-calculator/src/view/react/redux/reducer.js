
const reducer = (state, action) => {
    switch(action.type) {
        case "INIT":
            debugger;
            return state;
        
        default:
            return state;
    }
}

export default reducer;